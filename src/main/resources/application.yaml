ktor:
    application:
        modules:
            - com.bank.ApplicationKt.module
    deployment:
        port: 8080

db:
    jdbcUrl: jdbc:postgresql://localhost:5432/bank
    user: postgres
    password: postgres
